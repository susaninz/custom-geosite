# 🔄 Auto-Update Setup - download_geosite.sh

Настройка автоматического обновления `geosite.dat` из GitHub Releases.

---

## 📋 Что делает скрипт

`download_geosite.sh` автоматически:

1. ✅ Проверяет последний релиз на GitHub
2. ✅ Сравнивает с установленной версией
3. ✅ Скачивает новый `geosite.dat` если есть обновление
4. ✅ Создаёт backup старого файла
5. ✅ Заменяет файл в `/etc/openclash/`
6. ✅ Перезапускает OpenClash
7. ✅ Отправляет уведомление в Telegram

---

## 🚀 УСТАНОВКА

### Шаг 1: Подключитесь к роутеру

```bash
ssh root@192.168.31.1
# Пароль: 321678
```

### Шаг 2: Создайте скрипт на роутере

**На роутере:**

```bash
# Создайте файл
cat > /root/download_geosite.sh << 'EOF'
#!/bin/sh
# ... (весь код скрипта)
EOF

# Установите права
chmod 700 /root/download_geosite.sh
```

**ИЛИ загрузите с компьютера:**

```bash
# С вашего компьютера (Mac)
cd "/Users/ivanslezkin/Cursor/Open wrt router"

scp plugins/openclash/geosite-manager/router/download_geosite.sh root@192.168.31.1:/root/

# На роутере
chmod 700 /root/download_geosite.sh
```

### Шаг 3: Настройте cron

**На роутере:**

```bash
# Откройте crontab
crontab -e

# Добавьте строку:
# Auto-update geosite.dat (каждый час в :15)
15 * * * * /root/download_geosite.sh >> /tmp/geosite_update.log 2>&1

# Сохраните (Ctrl+O, Enter, Ctrl+X)
```

**Частота проверок:**
- `15 * * * *` - каждый час в :15 минут
- `*/2 * * * *` - каждые 2 часа
- `0 */6 * * *` - каждые 6 часов

### Шаг 4: Перезапустите cron

```bash
/etc/init.d/cron restart
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Запуск вручную

```bash
# Запустите скрипт
/root/download_geosite.sh

# Вы увидите:
# ==========================================
# Starting geosite.dat auto-update check
# ==========================================
# Current version: none (или v1.0.0)
# Checking GitHub for latest release...
# Latest release: v1.0.0-commit-640d414b
# ...
```

### Тест 2: Проверьте лог

```bash
cat /tmp/geosite_update.log
```

**Если обновление НЕ требуется:**
```
[2025-11-07 19:20:00] Current version: v1.0.0-commit-640d414b
[2025-11-07 19:20:05] Latest release: v1.0.0-commit-640d414b
[2025-11-07 19:20:05] Already up to date: v1.0.0-commit-640d414b
[2025-11-07 19:20:05] No update needed
```

**Если обновление найдено:**
```
[2025-11-07 19:20:00] Current version: none
[2025-11-07 19:20:05] Latest release: v1.0.0-commit-640d414b
[2025-11-07 19:20:05] Update available: none → v1.0.0-commit-640d414b
[2025-11-07 19:20:10] Downloading geosite.dat...
[2025-11-07 19:20:15] ✓ Downloaded successfully (91081 bytes)
[2025-11-07 19:20:15] ✓ Backup created: /etc/openclash/backups/geosite.dat.20251107_192015
[2025-11-07 19:20:16] ✓ File installed successfully
[2025-11-07 19:20:16] ✓ Version saved: v1.0.0-commit-640d414b
[2025-11-07 19:20:20] ✓ OpenClash restarted successfully
[2025-11-07 19:20:25] ✓ Notification sent successfully
[2025-11-07 19:20:25] ==========================================
[2025-11-07 19:20:25] ✓ Update completed successfully!
[2025-11-07 19:20:25] Version: none → v1.0.0-commit-640d414b
[2025-11-07 19:20:25] ==========================================
```

### Тест 3: Проверьте установленный файл

```bash
# Проверьте размер
ls -lh /etc/openclash/geosite.dat

# Ожидаемый результат:
# -rw-r--r-- 1 root root 89K Nov  7 19:20 /etc/openclash/geosite.dat

# Проверьте версию
cat /etc/openclash/.geosite_version

# Ожидаемый результат:
# v1.0.0-commit-640d414b9e9534abd07a4a1b573aa67d2fc5dcfe
```

### Тест 4: Проверьте OpenClash

```bash
# Проверьте статус
/etc/init.d/openclash status

# Проверьте лог
tail -30 /tmp/openclash.log

# Проверьте что используется новый файл
ls -lh /etc/openclash/
```

---

## 📱 TELEGRAM УВЕДОМЛЕНИЯ

После успешного обновления вы получите сообщение в Telegram:

```
🔄 Geosite обновлён!

📦 Версия: v1.0.0-commit-640d414b
💾 Размер: 91 KB
🤖 Роутер: OpenWrt

✅ OpenClash перезапущен
```

При ошибке:
```
❌ Ошибка обновления Geosite

Причина: Download failed
📦 Версия: v1.0.0-commit-640d414b
🤖 Роутер: OpenWrt

Проверьте логи на роутере
```

---

## 📂 ФАЙЛЫ И ДИРЕКТОРИИ

### Скрипт:
```
/root/download_geosite.sh          # Основной скрипт
```

### Данные:
```
/etc/openclash/geosite.dat         # Актуальный файл
/etc/openclash/.geosite_version    # Текущая версия
/etc/openclash/backups/            # Директория с бэкапами
```

### Логи:
```
/tmp/geosite_update.log            # Лог скрипта
/tmp/openclash.log                 # Лог OpenClash
```

### Бэкапы:
```
/etc/openclash/backups/geosite.dat.20251107_192015
/etc/openclash/backups/geosite.dat.20251107_150010
/etc/openclash/backups/geosite.dat.20251106_193022
...
```
*(хранятся последние 5 бэкапов)*

---

## 🔍 ПРОВЕРКА РАБОТЫ

### Проверьте что cron запускает скрипт:

```bash
# Просмотрите crontab
crontab -l

# Проверьте последнее выполнение
logread | grep cron | grep download_geosite | tail -5

# Проверьте время последнего обновления
ls -lt /tmp/geosite_update.log
```

### Просмотр статистики:

```bash
# Сколько раз запускался скрипт
grep "Starting geosite.dat auto-update" /tmp/geosite_update.log | wc -l

# Сколько раз обновлялся
grep "Update completed successfully" /tmp/geosite_update.log | wc -l

# Текущая версия
cat /etc/openclash/.geosite_version

# История версий
ls -lt /etc/openclash/backups/
```

---

## 🛠️ TROUBLESHOOTING

### Проблема: "curl: command not found" или "wget: command not found"

**Решение:**
```bash
opkg update
opkg install curl wget
```

### Проблема: "Permission denied"

**Решение:**
```bash
chmod 700 /root/download_geosite.sh
```

### Проблема: Скрипт не запускается по cron

**Проверьте:**
```bash
# Cron работает?
/etc/init.d/cron status

# Cron enabled?
/etc/init.d/cron enable
/etc/init.d/cron restart

# Правильный путь в crontab?
crontab -l | grep download_geosite

# Логи cron
logread | grep cron | tail -20
```

### Проблема: Скачивание не работает

**Проверьте:**
```bash
# Интернет работает?
ping -c 3 github.com

# GitHub доступен?
curl -I https://api.github.com/repos/susaninz/openwrtrouter/releases/latest

# Правильный URL в скрипте?
grep GITHUB_REPO /root/download_geosite.sh
```

### Проблема: Файл скачался но OpenClash не перезапустился

**Решение:**
```bash
# Перезапустите вручную
/etc/init.d/openclash restart

# Проверьте статус
/etc/init.d/openclash status

# Проверьте что OpenClash установлен
ls -la /etc/init.d/openclash
```

### Проблема: Telegram уведомление не приходит

**Проверьте:**
```bash
# Railway доступен?
curl -I https://openwrtrouter-production.up.railway.app/health

# Правильные credentials в скрипте?
grep WEBHOOK_URL /root/download_geosite.sh
grep WEBHOOK_SECRET /root/download_geosite.sh

# Лог показывает отправку?
grep "Notification sent" /tmp/geosite_update.log
```

---

## 🔄 РУЧНОЕ ОБНОВЛЕНИЕ

Если хотите обновить немедленно:

```bash
# Просто запустите скрипт
/root/download_geosite.sh

# Или принудительно скачайте (даже если версия та же)
# Удалите файл версии
rm /etc/openclash/.geosite_version

# Запустите скрипт
/root/download_geosite.sh
```

---

## 📝 ВОССТАНОВЛЕНИЕ ИЗ БЭКАПА

Если что-то пошло не так:

```bash
# Посмотрите доступные бэкапы
ls -lt /etc/openclash/backups/

# Восстановите последний бэкап
cp /etc/openclash/backups/geosite.dat.20251107_192015 /etc/openclash/geosite.dat

# Перезапустите OpenClash
/etc/init.d/openclash restart

# Откорректируйте версию (если нужно)
echo "v1.0.0-old" > /etc/openclash/.geosite_version
```

---

## ⚙️ НАСТРОЙКА

### Изменить частоту проверок:

**Редактируйте crontab:**
```bash
crontab -e

# Варианты:
15 * * * *      # Каждый час в :15 минут (рекомендуется)
0 */2 * * *     # Каждые 2 часа
0 */6 * * *     # Каждые 6 часов
0 9,15,21 * * * # В 9:00, 15:00, 21:00
```

### Изменить webhook URL/secret:

**Редактируйте скрипт:**
```bash
nano /root/download_geosite.sh

# Найдите и измените:
WEBHOOK_URL="https://openwrtrouter-production.up.railway.app"
WEBHOOK_SECRET="ваш_secret"
```

### Изменить количество хранимых бэкапов:

**Редактируйте скрипт:**
```bash
nano /root/download_geosite.sh

# Найдите строку:
ls -t "$BACKUP_DIR"/geosite.dat.* | tail -n +6 | xargs rm -f

# Измените 6 на нужное число + 1
# (например, 11 для хранения 10 бэкапов)
```

---

## 📊 СТАТИСТИКА

### Ожидаемые параметры:

| Параметр | Значение |
|----------|----------|
| Частота проверок | 24 раза в сутки (каждый час) |
| Частота обновлений | 1-2 раза в неделю |
| Размер скачивания | ~91 KB |
| Время скачивания | 1-2 секунды |
| Время перезапуска | 5-10 секунд |
| Общее время | ~20-30 секунд |

### Использование ресурсов:

| Ресурс | Использование |
|--------|---------------|
| CPU | Минимальное (~1-2% на 30 сек) |
| RAM | Минимальное (~1-2 MB временно) |
| Диск | ~500 KB (5 бэкапов по 91 KB) |
| Сеть | ~91 KB при обновлении |

---

## ✅ CHECKLIST

- [ ] Скрипт загружен на роутер
- [ ] Права установлены (chmod 700)
- [ ] Crontab настроен
- [ ] Cron перезапущен
- [ ] Тестовый запуск успешен
- [ ] Файл скачался и установился
- [ ] OpenClash перезапустился
- [ ] Telegram уведомление пришло
- [ ] Версия сохранилась
- [ ] Бэкап создался

---

## 🎉 ГОТОВО!

После выполнения всех шагов:

✅ Роутер автоматически проверяет обновления каждый час  
✅ Скачивает и устанавливает новые версии  
✅ Создаёт бэкапы перед обновлением  
✅ Перезапускает OpenClash после установки  
✅ Отправляет уведомления в Telegram  

**Ваша система полностью автоматизирована!** 🚀

---

## 🔗 ССЫЛКИ

- **GitHub Releases:** https://github.com/susaninz/openwrtrouter/releases
- **Railway Bot:** https://openwrtrouter-production.up.railway.app
- **Telegram Bot:** @openwrtrouterbot
- **Документация:** `/plugins/openclash/geosite-manager/`


