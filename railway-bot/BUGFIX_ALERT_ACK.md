# 🐛 Bugfix: Alert "Прочитано" Button

**Дата:** 2025-11-07  
**Версия:** v1.0.3-alert-ack-fixed  
**Статус:** ✅ Исправлено и задеплоено

---

## 🔍 Проблема

**Симптомы:**
- ❌ Кнопка "✅ Прочитано" в критических алертах не реагировала
- ❌ Оставался индикатор загрузки (spinning indicator)
- ❌ Кнопка выглядела "зависшей"

**Пример алерта:**
```
🟡 КРИТИЧЕСКИЙ АЛЕРТ!

🔥 CPU
📊 Текущее: 3.34
⚠️ Порог: 85
📈 Превышение: -96.1%

🕐 2025-11-07T20:30

[✅ Прочитано] ← не работала
```

---

## 🔎 Причина

**Технически:**
- Отсутствовал вызов `answerCallbackQuery` для callback query
- Telegram API требует ответ на каждый callback query
- Без ответа кнопка остается в состоянии "loading"

**Код (до исправления):**
```python
elif callback_data == 'alert_ack':
    requests.post(f"{TELEGRAM_API}/editMessageReplyMarkup", json={
        'chat_id': chat_id,
        'message_id': message_id,
        'reply_markup': {'inline_keyboard': [[{"text": "✅ Прочитано", "callback_data": "none"}]]}
    })
```

**Проблема:** Обновляет кнопки, но НЕ отвечает на callback query.

---

## ✅ Решение

**Добавлено:**
1. `answerCallbackQuery` для снятия loading state
2. Текст подтверждения: "✅ Алерт отмечен как прочитанный"
3. Timeout для надёжности

**Код (после исправления):**
```python
elif callback_data == 'alert_ack':
    # Answer callback to show "Принято" notification
    requests.post(f"{TELEGRAM_API}/answerCallbackQuery", json={
        'callback_query_id': callback_id,
        'text': '✅ Алерт отмечен как прочитанный',
        'show_alert': False
    }, timeout=10)
    
    # Update button to show it was acknowledged
    requests.post(f"{TELEGRAM_API}/editMessageReplyMarkup", json={
        'chat_id': chat_id,
        'message_id': message_id,
        'reply_markup': {'inline_keyboard': [[{"text": "✅ Прочитано", "callback_data": "none"}]]}
    }, timeout=10)
```

---

## 🎯 Результат

### До
- ❌ Кнопка не реагирует
- ❌ Индикатор загрузки
- ❌ Нет обратной связи

### После
- ✅ Кнопка работает мгновенно
- ✅ Показывается всплывающее уведомление: "✅ Алерт отмечен как прочитанный"
- ✅ Кнопка меняется на "✅ Прочитано" (неактивная)
- ✅ Нет индикатора загрузки

---

## 🚀 Деплой

**GitHub:**
- Commit: `5a63719` - fix(bot): alert_ack button not responding
- Commit: `26d2906` - chore: bump version to 1.0.3-alert-ack-fixed

**Railway:**
- URL: https://openwrtrouter-production.up.railway.app
- Status: ✅ Deployed
- Version: `v1.0.3-alert-ack-fixed`
- Deploy time: ~75 секунд

**Проверка:**
```bash
curl https://openwrtrouter-production.up.railway.app/health
{
  "status": "healthy",
  "version": "1.0.3-alert-ack-fixed",
  "timestamp": "2025-11-07T20:46:XX",
  "metrics_count": 1
}
```

---

## 📋 Тестирование

### Как проверить

1. Дождаться следующего критического алерта от роутера
2. Или триггернуть алерт вручную через webhook

### Тестовый webhook
```bash
curl -X POST https://openwrtrouter-production.up.railway.app/webhook/alert \
  -H "Authorization: Bearer YOUR_SECRET" \
  -H "Content-Type: application/json" \
  -d '{
    "severity": "critical",
    "type": "CPU",
    "value": 95,
    "threshold": 85,
    "message": "Тестовый алерт"
  }'
```

### Ожидаемое поведение
1. ✅ Получен алерт в Telegram
2. ✅ Нажатие на "✅ Прочитано"
3. ✅ Показывается уведомление: "✅ Алерт отмечен как прочитанный"
4. ✅ Кнопка становится неактивной: "✅ Прочитано"

---

## 🔗 Связанные файлы

- **Файл:** `plugins/openclash/geosite-manager/railway-bot/app.py`
- **Строки:** 560-573
- **Endpoint:** `/telegram/webhook`
- **Callback:** `alert_ack`

---

## 📚 Уроки

### Telegram Bot API

**Важно:**
- **Всегда** отвечайте на callback queries через `answerCallbackQuery`
- Без ответа кнопка остается в loading state
- `show_alert: false` - короткое уведомление сверху
- `show_alert: true` - модальный alert

**Хорошая практика:**
```python
# 1. Ответить на callback (обязательно!)
requests.post(f"{TELEGRAM_API}/answerCallbackQuery", json={
    'callback_query_id': callback_id,
    'text': 'Действие выполнено'
})

# 2. Обновить интерфейс (опционально)
requests.post(f"{TELEGRAM_API}/editMessageText", ...)
```

---

## ✅ Checklist

- [x] Баг идентифицирован
- [x] Причина найдена
- [x] Решение реализовано
- [x] Код закоммичен
- [x] Задеплоен на Railway
- [x] Версия обновлена (v1.0.3)
- [x] Документация создана

---

**Статус:** ✅ **FIXED**  
**Версия:** v1.0.3-alert-ack-fixed  
**Дата:** 2025-11-07

